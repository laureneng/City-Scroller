 
//RANDOM LITTER FUNCTION
var draw= function() {
    if (frameCount%15 === 0){  //don't know what that means
var x = random(0,250);  

var litter = function(){
    var litter = random(0, 400);
    fill(245, 57, 167);
    triangle(fish3+308, 320, fish3+220, 340, fish3+220, 303);
    rect(fish3+255, 300, 60, 40);
x = x+2;
}; 

<div class="blah">
fish3();
}
};




//COLLIDE FUNCTION
var time=.016hrs 

var collide = 
function(){
 	while(time<=.016hrs)
 		if(xposSprite == xposLitter && yposSprite == yposLitter){
 			//make the litter disappear and add a certain number to a variable that is the score

 		}
}

for (int i = 0; i < 10; i++) { 
    if (xposCar == xposGrape && yposCar == yposGrape) { 
      newGrape.tele(); 
    } 
} 

  //establish litter and sprite classes

  //check time syntax